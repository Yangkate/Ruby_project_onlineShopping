<%= form_for(@city, html: {class: "form-horizontal"}) do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="form-group">
    <div class="col-lg-2"><%= f.label :city_name %></div>
    <div class="col-lg-5"><%= f.text_field :city_name, :class => "form-control", :required => true %></div>
  </div>
  <div class="form-group">
    <div class="col-lg-2"><%= f.label :Country_id %></div>
    <div class="col-lg-5"><%= f.collection_select(:Country_id, Country.all.order("country_name"), :id, :country_name , :class => "form-control") %></div>
  </div>

    <div class="form-group">
      <div class="col-lg-3"></div>
      <div class="col-lg-9 text-right">
        <%= f.submit "Submit", :class => "btn btn-primary" %> &nbsp;&nbsp;
        <%= link_to 'Back', cities_path, :class => "btn btn-default" %>
      </div>
    </div>
<% end %>
